__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.ruff_cache/
.mypy_cache/
.DS_Store

# Credentials / session — never commit. These live in ~/.config/tg-cli anyway,
# but guard against a stray copy landing in the repo.
*.session
*.session-journal
config.json

# Built artifacts
build/
dist/
