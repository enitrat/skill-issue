# UPDATE PROGRESS — Pass {props.currentPass}

## Objective

Rewrite PROGRESS.md with the current state of the project.
This is a READ-ONLY assessment — do NOT run builds or tests.

## Phase Definitions

{props.phaseDefinitions}

## Current Phase Statuses

{props.phaseStatuses}

Total phases: {props.totalPhases}

## Project-Specific Instructions

{props.projectInstructions}

## Generic Progress Steps

1. **Read current PROGRESS.md** (if it exists)
2. **Read `git log --oneline -30`** for recent commits
3. **Browse the source tree** — look at what files exist
4. **Read key source files** to assess completion (don't run anything)
5. **Rewrite PROGRESS.md** with a progress table and summary
6. **Commit**: `docs: update PROGRESS.md (pass {props.currentPass})`

## CRITICAL

- Do NOT run builds or tests — this is assessment only
- Be honest about completion — don't inflate percentages
- Note blockers and dependencies clearly
