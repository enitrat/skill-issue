[project]
name = "tts-notify-hook"
version = "0.1.0"
description = "Claude Code hook to convert responses to speech using Kyutai Pocket TTS"
requires-python = ">=3.11"
dependencies = [
    "pocket-tts",
    "pygame>=2.6.1",
    "scipy",
]
