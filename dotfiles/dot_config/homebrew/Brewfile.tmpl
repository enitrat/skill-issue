{{ if eq .chezmoi.os "darwin" -}}
# Brewfile — managed by chezmoi (skill-issue). Edits here are overwritten.
#
# Scope rule: macOS GUI casks, plus formulae mise cannot provide. Every
# cross-platform CLI tool belongs in the mise tool list instead.

# --- GUI apps --------------------------------------------------------------
cask "raycast"
cask "cursor"
cask "tailscale"
cask "amphetamine"    # unlike caffeinate, its closed-lid mode actually holds
cask "hiddenbar"      # hide/reveal menu-bar icons around the notch

# Free for personal use; commercial use needs a paid license.
cask "orbstack"

# --- Fonts -----------------------------------------------------------------
cask "font-meslo-lg-nerd-font"   # installs under the name "MesloLGS NF"

# --- Homebrew-only formulae ------------------------------------------------
brew "mosh"           # roaming-friendly SSH for flaky connections
brew "gcc@15"
brew "llvm@19"        # for Cairo-Native
{{ else -}}
# Brewfile is macOS-only; nothing to install on this OS.
{{ end -}}
