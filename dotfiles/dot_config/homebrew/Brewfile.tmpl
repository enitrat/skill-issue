# Generated by chezmoi from .chezmoidata/packages.toml. Edits are overwritten.
#
# Scope rule: macOS GUI casks, plus formulae mise cannot provide. Every
# cross-platform CLI tool belongs in the [[mise]] section of packages.toml.

{{ range .cask -}}
cask "{{ .name }}"{{ if hasKey . "note" }}  # {{ .note }}{{ end }}
{{ end }}
{{ range .formula -}}
brew "{{ .name }}"{{ if hasKey . "note" }}  # {{ .note }}{{ end }}
{{ end -}}
