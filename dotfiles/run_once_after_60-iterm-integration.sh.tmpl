#!/bin/bash
set -euo pipefail
{{ template "lib.sh" . }}

# iTerm2's cwd tracking and jump-to-prompt are shell-hook based, so every
# remote box needs its own copy -- the client cannot supply them.
if [ -f "$HOME/.iterm2_shell_integration.zsh" ]; then
    log iterm2 "already installed"
else
    curl -fsSL https://iterm2.com/shell_integration/zsh -o "$HOME/.iterm2_shell_integration.zsh"
    log iterm2 "installed"
fi
