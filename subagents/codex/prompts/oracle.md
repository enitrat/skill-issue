---
description: Run Oracle sub-agent (mapped to default)
argument-hint: "<research task>"
---
Use `spawn_agent` with `agent_type:"default"` for this task:

$ARGUMENTS

Requirements:
- Focus on external research: docs, APIs, best practices, and web sources.
- Prefer primary sources and include links.
- Write full findings to `./.codex/cache/agents/oracle/latest-output.md` (project-local).
- Return a short summary here plus that output path.
